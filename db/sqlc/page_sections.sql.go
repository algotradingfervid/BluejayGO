// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: page_sections.sql

package sqlc

import (
	"context"
)

const getPageSection = `-- name: GetPageSection :one

SELECT id, page_key, section_key, heading, subheading, description, label, primary_button_text, primary_button_url, secondary_button_text, secondary_button_url, is_active, display_order, created_at, updated_at FROM page_sections
WHERE page_key = ? AND section_key = ? AND is_active = 1
`

type GetPageSectionParams struct {
	PageKey    string `json:"page_key"`
	SectionKey string `json:"section_key"`
}

// ====================================================================
// PAGE SECTIONS QUERY FILE
// ====================================================================
// This file contains SQL queries for managing configurable page sections
// (heroes, CTAs, testimonials, stats, etc.) across different pages.
//
// Entity: page_sections table
// Purpose: Store content for modular page sections that can be edited via admin
// Features:
//   - Multi-page support via page_key (e.g., "homepage", "about", "contact")
//   - Section-specific content via section_key (e.g., "hero", "cta", "stats")
//   - Flexible content fields (heading, subheading, description, buttons, etc.)
//   - Visibility control via is_active flag
//   - Display ordering for multiple sections on same page
//
// Common page_key values: homepage, about, products, solutions, contact
// Common section_key values: hero, cta, stats, testimonials, features
// ====================================================================
// Retrieves a specific active section for a given page.
//
// Parameters:
//
//	$1 (TEXT) - page_key: Page identifier (e.g., "homepage", "about")
//	$2 (TEXT) - section_key: Section identifier (e.g., "hero", "cta")
//
// Returns: PageSection - Single section record or error if not found/inactive
//
// Filtering logic:
//   - page_key = ? - Matches specific page
//   - section_key = ? - Matches specific section type
//   - is_active = 1 - Only returns visible sections
//
// Use case: Fetching hero content for homepage, CTA for contact page, etc.
func (q *Queries) GetPageSection(ctx context.Context, arg GetPageSectionParams) (PageSection, error) {
	row := q.db.QueryRowContext(ctx, getPageSection, arg.PageKey, arg.SectionKey)
	var i PageSection
	err := row.Scan(
		&i.ID,
		&i.PageKey,
		&i.SectionKey,
		&i.Heading,
		&i.Subheading,
		&i.Description,
		&i.Label,
		&i.PrimaryButtonText,
		&i.PrimaryButtonUrl,
		&i.SecondaryButtonText,
		&i.SecondaryButtonUrl,
		&i.IsActive,
		&i.DisplayOrder,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getPageSectionByID = `-- name: GetPageSectionByID :one
SELECT id, page_key, section_key, heading, subheading, description, label, primary_button_text, primary_button_url, secondary_button_text, secondary_button_url, is_active, display_order, created_at, updated_at FROM page_sections WHERE id = ?
`

// Retrieves a single page section by its primary key ID (ignores is_active).
//
// Parameters:
//
//	$1 (INTEGER) - section ID
//
// Returns: PageSection - Single section record or error if not found
//
// Use case: Admin editing, fetching section for update regardless of active status
// Note: Does NOT filter by is_active, so returns draft/inactive sections
func (q *Queries) GetPageSectionByID(ctx context.Context, id int64) (PageSection, error) {
	row := q.db.QueryRowContext(ctx, getPageSectionByID, id)
	var i PageSection
	err := row.Scan(
		&i.ID,
		&i.PageKey,
		&i.SectionKey,
		&i.Heading,
		&i.Subheading,
		&i.Description,
		&i.Label,
		&i.PrimaryButtonText,
		&i.PrimaryButtonUrl,
		&i.SecondaryButtonText,
		&i.SecondaryButtonUrl,
		&i.IsActive,
		&i.DisplayOrder,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listAllPageSections = `-- name: ListAllPageSections :many
SELECT id, page_key, section_key, heading, subheading, description, label, primary_button_text, primary_button_url, secondary_button_text, secondary_button_url, is_active, display_order, created_at, updated_at FROM page_sections ORDER BY page_key, display_order ASC
`

// Retrieves all page sections across all pages, grouped and ordered.
//
// Parameters: none
// Returns: []PageSection - Array of all sections across entire site
//
// Sorting logic:
//  1. page_key - Groups sections by page (alphabetical)
//  2. display_order ASC - Orders sections within each page
//
// Use case: Admin section management dashboard, bulk operations, site-wide overview
// Note: Returns ALL sections including inactive ones (is_active not filtered)
func (q *Queries) ListAllPageSections(ctx context.Context) ([]PageSection, error) {
	rows, err := q.db.QueryContext(ctx, listAllPageSections)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []PageSection{}
	for rows.Next() {
		var i PageSection
		if err := rows.Scan(
			&i.ID,
			&i.PageKey,
			&i.SectionKey,
			&i.Heading,
			&i.Subheading,
			&i.Description,
			&i.Label,
			&i.PrimaryButtonText,
			&i.PrimaryButtonUrl,
			&i.SecondaryButtonText,
			&i.SecondaryButtonUrl,
			&i.IsActive,
			&i.DisplayOrder,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPageSections = `-- name: ListPageSections :many
SELECT id, page_key, section_key, heading, subheading, description, label, primary_button_text, primary_button_url, secondary_button_text, secondary_button_url, is_active, display_order, created_at, updated_at FROM page_sections
WHERE page_key = ? AND is_active = 1
ORDER BY display_order ASC
`

// Retrieves all active sections for a specific page in display order.
//
// Parameters:
//
//	$1 (TEXT) - page_key: Page identifier
//
// Returns: []PageSection - Array of active sections for the page
//
// Filtering: is_active = 1 - Only visible sections
// Sorting: display_order ASC - Sections appear in configured order
//
// Use case: Rendering all sections for a page, building page content dynamically
func (q *Queries) ListPageSections(ctx context.Context, pageKey string) ([]PageSection, error) {
	rows, err := q.db.QueryContext(ctx, listPageSections, pageKey)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []PageSection{}
	for rows.Next() {
		var i PageSection
		if err := rows.Scan(
			&i.ID,
			&i.PageKey,
			&i.SectionKey,
			&i.Heading,
			&i.Subheading,
			&i.Description,
			&i.Label,
			&i.PrimaryButtonText,
			&i.PrimaryButtonUrl,
			&i.SecondaryButtonText,
			&i.SecondaryButtonUrl,
			&i.IsActive,
			&i.DisplayOrder,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updatePageSection = `-- name: UpdatePageSection :exec
UPDATE page_sections SET
    heading = ?,
    subheading = ?,
    description = ?,
    label = ?,
    primary_button_text = ?,
    primary_button_url = ?,
    secondary_button_text = ?,
    secondary_button_url = ?,
    is_active = ?,
    updated_at = CURRENT_TIMESTAMP
WHERE id = ?
`

type UpdatePageSectionParams struct {
	Heading             string `json:"heading"`
	Subheading          string `json:"subheading"`
	Description         string `json:"description"`
	Label               string `json:"label"`
	PrimaryButtonText   string `json:"primary_button_text"`
	PrimaryButtonUrl    string `json:"primary_button_url"`
	SecondaryButtonText string `json:"secondary_button_text"`
	SecondaryButtonUrl  string `json:"secondary_button_url"`
	IsActive            bool   `json:"is_active"`
	ID                  int64  `json:"id"`
}

// Updates the content fields of an existing page section.
//
// Parameters:
//
//	$1 (TEXT) - heading: Main heading/title for the section
//	$2 (TEXT) - subheading: Secondary heading or subtitle
//	$3 (TEXT) - description: Longer description or body text
//	$4 (TEXT) - label: Short label or tag (e.g., "New", "Featured")
//	$5 (TEXT) - primary_button_text: Text for primary CTA button
//	$6 (TEXT) - primary_button_url: URL for primary button
//	$7 (TEXT) - secondary_button_text: Text for secondary button
//	$8 (TEXT) - secondary_button_url: URL for secondary button
//	$9 (BOOLEAN) - is_active: Visibility flag (1=visible, 0=hidden)
//	$10 (INTEGER) - id: Section ID to update
//
// Returns: (none) - sqlc annotation :exec returns only row count
//
// Note: updated_at is automatically set to CURRENT_TIMESTAMP to track modifications
// Use case: Admin content editing for heroes, CTAs, and other configurable sections
//
// Content structure supports common section patterns:
//   - Hero: heading, subheading, description, primary_button (CTA)
//   - CTA: heading, description, primary_button, secondary_button
//   - Feature: label, heading, description
func (q *Queries) UpdatePageSection(ctx context.Context, arg UpdatePageSectionParams) error {
	_, err := q.db.ExecContext(ctx, updatePageSection,
		arg.Heading,
		arg.Subheading,
		arg.Description,
		arg.Label,
		arg.PrimaryButtonText,
		arg.PrimaryButtonUrl,
		arg.SecondaryButtonText,
		arg.SecondaryButtonUrl,
		arg.IsActive,
		arg.ID,
	)
	return err
}
